var Mm = require('./lib');
var fs = require('fs');
var thefile = "node-musicmetadata-broken.m4a"

var parser = new Mm(fs.createReadStream("./test/samples/oggy.ogg"));
parser.on('metadata', function(metadata) {
  console.log("metadata", metadata);
});
parser.on('done', function(value) {
    console.log(value);
});

console.log(parser.listeners('metadata'));