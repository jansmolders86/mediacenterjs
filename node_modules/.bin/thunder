#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../thunder/bin/thunder" "$@"
  ret=$?
else 
  node  "$basedir/../thunder/bin/thunder" "$@"
  ret=$?
fi
exit $ret
